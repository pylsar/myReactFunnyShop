import React from 'react'

import './Header.scss'

const Home = () => {
    return (
        <header className="header">
            <nav>
                <a href="/">Главная</a>
            </nav>
        </header>
    )
}

export default Home
